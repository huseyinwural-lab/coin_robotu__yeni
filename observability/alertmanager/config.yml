global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by: [alertname, category, component]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: default
    webhook_configs:
      - url: http://localhost:65535/alerts-disabled
        send_resolved: true
